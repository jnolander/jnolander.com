browser support:
transform

Recognition:
pixel font

resize also triggered when mobile address bar dissapears

move up the next button when height is more.

all buttons should be looked over click/ touch events. focus etc..
same applies to the form

e.target only the parent! applies to portfolio items!

form validation javascript.

remove verbose debugging from PHP mailer

img also needs alt-text and whatever

links in other section

meta tags
